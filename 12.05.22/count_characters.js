//* transformar a string em array
//* captar elemento por elemento no array
//* testar elemento por elemento
//* encontrar iguais e contar


function count(string) {
  const charsArray = string.split('')

  const (for char of charsArray) {

  }

  charsArray.every(() => { })

  const result = {}
  let count = 0
  for (let i = 0; i < charsArray.length; i++) {

    if (string.includes(charsArray[i]) && ) {
      // console.log('char')
      count++
    } else {
      console.log()
    }
  }
  console.log(count)

  // console.log(charsArray)
  // return {}
}

count('aba')
